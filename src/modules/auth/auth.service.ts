export { generateOtp, verifyOtp, deleteOtp } from "@/services/otp.service";
export {
    AccountType,
    generateTokens,
    signToken,
    verifyToken,
    type Payload,
} from "@/services/token.service";

